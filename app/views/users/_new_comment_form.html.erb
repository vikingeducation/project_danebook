<%= form_for post.comments.build, url: post_comments_path(post) do |f|  %>
  <div class="col-xs-10 col-xs-offset-1">
    <%= text_area_tag("comment[description]", "", class: "btn-block form-control new-comment-input", placeholder: "Penny for your thoughts...") %>
  </div>
  <div class="col-xs-6 col-xs-offset-3">
    <%= submit_tag("Comment", class: "btn btn-primary btn-block") %>
  </div>
<% end %>
