
console.log("Entering here");
var formRow = "<%= j( render partial: 'post_form', locals: { post: @post } ) %>"
var postRow = "<%= j( render partial: 'post', locals: { post: @post } ) %>"

var $formRow = $(formRow);
var $postRow = $( postRow );


var $form_display = $(".new-post-form");
var $post_display = $(".post-display");

$form_display.empty();
$form_display.append( $formRow);
$post_display.prepend( $postRow );

console.log("Leaving here");