<div class="col-md-4 photo">
  <%= link_to image_tag(photo.image.url(:thumb)), user_photo_path(@user, photo) %>
  <div class = "photo-upload-date">
    <p>Uploaded <%=photo.created_at.to_formatted_s %></p>
  </div>
</div>