(function(){
  var target, type, msg = [
    <% @msg.each do |msg| %>
      "<%= j(msg) %>",
    <% end %>
  ];
  <% if defined?(@target) %>
    target = <%= @target %>;
    type = "<%= @target_type %>";
  <% end %>
  DANEBOOK.Flashes("<%= j(@status.to_s) %>", msg, target, type)
})()
