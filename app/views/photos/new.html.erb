<div id="static_pages">

  <%= render 'shared/navbar' %>
  <%= render 'shared/banner', user: @user %>

  <div class="container section-container">
    <%= render 'shared/flash' %>
    <div class="row section-header">
      <div class="col-sm-8 col-sm-offset-2">
        <h2 class="text-center">Photo</h2>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-10 col-sm-offset-1 photo-container">
          <%= form_for @user.photo_posts.last do |f| %>
          <%= f.file_field(:file, class: "form-control btn-block") %>
          <%= f.submit(class: "btn btn-primary btn-block") %>
          <% end %>
          <%# TODO: Add styling to photos new page %>
        </p>
      </div>
    </div><!-- Photo Wrapper -->
  </div><!-- Contentt Section -->
</div>
