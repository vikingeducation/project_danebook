// remove comment
var cid = "<%= @comment.id %>"
$('.existing [data-cid="' + cid + '"]').parent().remove().slideUp()

// flash message
$("#flash-messages").html("<%= j(render partial: 'shared/flash') %>").slideDown();
<% flash.clear %>