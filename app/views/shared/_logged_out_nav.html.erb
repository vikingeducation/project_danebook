<nav class="nav navbar-default navbar-fixed-top navbar-custom">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">
                <img class="logo" src="/assets/danish_flag.gif">
            </a>
            <a class="navbar-brand" href="#">Danebook</a>
        </div>
        <div class="pull-right">
            <%= simple_form_for :user, url: session_path, html: { class: "inline-form"} do |f| %>
            <%= f.input_field :email, placeholder: 'Email', label: false %>
            <%= f.input_field :password, placeholder: 'password', label: false %>
            <%= check_box_tag :remember_me, inline_label: 'Remember' %>
            <%= f.button :submit, "Login" %>
            <% end %>
        </div>
    </div>
</nav>
