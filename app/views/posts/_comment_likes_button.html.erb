 <%=  if comment.liked_by?(current_user.id)
      link_to "Unlike", comment_comment_like_path(comment, current_user.id), method: :delete
      else
       render 'comment_likes_form', comment: comment 
     end
%>