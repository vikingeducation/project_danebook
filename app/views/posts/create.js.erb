var PostHtml = "<%= j(render partial: 'shared/post_show', locals: { post: @post, user: current_user} ) %>";
var $postList = $("#post-list");

$postList.prepend(PostHtml);
var newPostHtml =$postList.children().first();
newPostHtml.hide();
newPostHtml.slideDown(3000);
