<nav class="navbar navbar-default navbar-fixed-top danebook-navbar">
		<div class="container">
			<div class="row">
				<div class="col-sm-1 col-xs-12">
					<div class="navbar-brand" id="danebook-brand"aria-expanded="true">
					    <% if current_user %>
							<%= link_to (image_tag "logo.gif", class:"img-circle danebook-logo"), user_posts_path(current_user, newsfeed:true) %>
						<% else %>
							<%= link_to (image_tag "logo.gif", class:"img-circle danebook-logo"), new_user_path %>
						<% end %>
					</div>
					<button type="button" id="collapse-button"class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
						<!-- These spans draw the lines inside the button -->
						<span class="sr-only">Toggle Navigation</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</button>
				</div>
				<div id="navbar"  class="navbar-collapse collapse">
					<% if current_user %>
					<div class="col-sm-6 col-xs-12 col-sm-offset-2">
					<%= form_tag users_path, method: "get" do %>
					    <%= text_field_tag :name, '', class: "form-control danebook-search", placeholder: "Search for Users" %>
					    <%= submit_tag :name, {id: "hidden_search_button"} %>
					<% end %>

					</div>
					<div class="dropdown pull-right hidden-xs">
						<button class="danebook-navbar-text dropdown-toggle button" type="button" id="dropdownMenu1" data-toggle="dropdown">
						  <%= current_user.first_name %>
						  <span class="caret"></span>
						</button>
						<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu1">
						  <li class="menu-item" role="presentation">
						    <%= link_to "Sign Out", logins_path, method: "delete", role: "menuitem", tabindex: "-1" %>
						    <%= link_to "View Timeline", user_posts_path(current_user, newsfeed: "false") %>
						  </li>
						</ul>
					</div>
					<div class="col-xs-12 hidden-sm hidden-md hidden-lg">
						<%= link_to "Sign Out", logins_path, method: "delete", class: "btn btn-block btn-primary", tabindex: "-1" %>
					</div>
					<div class="col-xs-12 hidden-sm hidden-md hidden-lg">
					    <%= link_to "View Timeline", user_posts_path(current_user, newsfeed: "false"), class: "btn btn-block btn-primary" %>
					</div>
					<% else %>
					<div class="col-sm-2 col-xs-10 col-xs-offset-1">
					<%= form_tag users_path, method: "get" do %>
					    <%= text_field_tag :name, '', class: "form-control danebook-search", placeholder: "Search for Users" %>
					    <%= submit_tag :name, {id: "hidden_search_button"} %>
					<% end %>

					</div>
					  <%= render partial: "logins/login" %>
					<% end %>
				</div>
			</div>
		</div>
	</nav>
