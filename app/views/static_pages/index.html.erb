<%= render :partial => 'shared/nav' %>
  
<div class="row">
  
  <div class="col-xs-12">
    <ul class="nav nav-tabs">
      <%
      [
        ['About', current_user ? user_path(current_user) : '#'],
        ['About Edit', '/about_edit'],
        ['Friends', '/friends'],
        ['News Feed', '/news_feed'],
        ['Photos', '/photos'],
        ['Signup', '/signup'],
        ['Timeline', '/timeline'],
        ['Sign Out', session_path, :method => :delete]
      ].each do |link_args|
      %>
        <li role="presentation">
          <%= link_to(*link_args) %>
        </li>
      <% end %>
    </ul>
  </div>

</div>
