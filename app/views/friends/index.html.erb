   
   <div class="container">
   <%= render "/shared/second_nav" %>
   <!-- Current location header -->
          <section class="nav-about">
              <h2>
              Friends
              </h2>
          </section>

          <!-- Main -->
          <main class="main-about">
              <div class="row"> 
               <% @friends.each do |friend| %>
                    <%= render "/shared/friend_display", friend: friend%>
                <% end %>
              </div>
          </main>
      </div>
