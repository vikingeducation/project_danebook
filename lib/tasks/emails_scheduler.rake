namespace :emails_scheduler do

  desc "Send email with comments"
  #   every 1.hour do
  #     if get_latest_comments()
  #
  #   def get_latest_comments
  #     latest_comments = []
  #     user.posts.each {|p| latest_comments << p.comments.where('created_at > ?', 1.hours.ago)}
  #   end
  #     task send_comment_udpates_email
  # end

end
