<%= simple_form_for current_user.comments.build, url: user_comments_path(current_user, commentable_id: commentable.id, commentable_type: commentable.class), remote: true do |c| %>
<%= c.input :body, as: :text, label: false %>
<%= c.submit "Add Comment", class: "btn btn-sm"%>
<% end %>
<br>
