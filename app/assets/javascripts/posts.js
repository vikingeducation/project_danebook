var DANEBOOK = DANEBOOK || {}


DANEBOOK.PostModule = (function () {
/*
  function init() {
    //_addListeners();
    //$('.comment-form').hide();
  };

  function _addListeners() {
    //$('#timeline, #photo-show, #newsfeed').on('click', "[data-method='delete']", _delete);
  };

  function _delete() {

    //var $wrapper = $( event.target.closest('[data-type]') );
    //$wrapper.children('.comment-form, .comment-opener').toggle(250);
  };


  return {
    init: init,
  }
*/

})();