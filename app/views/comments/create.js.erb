var $comment = $("<%= j(render partial: '/comments/comment', locals: {comment: @comment, commentable: @commentable}) %>")

$(".<%= @commentable.class.to_s.downcase %>-comments[data-id='<%= @commentable.id %>']").append($comment);
$comment.hide().slideDown();
