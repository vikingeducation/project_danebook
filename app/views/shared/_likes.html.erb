
<!-- one like -->
<% if likes.count == 1 %>
<%= link_to "#{likes.first.user.first_name} #{likes.first.user.last_name}",user_timeline_path(likes.first.user) %> likes this.


<!-- more than one like -->
<% elsif likes.count == 2 %>

<%= link_to "#{likes.first.user.first_name} #{likes.first.user.last_name}", user_timeline_path(likes.first.user) %> and 1 other like this.


<% else %>

<%= link_to "#{likes.first.user.first_name} #{likes.first.user.last_name}", user_timeline_path(likes.first.user) %> and <%= likes.count - 1 %> others like this.


<% end %>