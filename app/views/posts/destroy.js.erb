console.log('inside destroy.js.erb');
// console.log('this is the id: ' + @post.id);

//remove the deleted post
$post = $('article[data-id=post-<%= @post.id%>]');
$post.slideUp(500, function(){this.remove();});

// PostModule.displayFlash();

$("#flash-messages").html("<%= escape_javascript(render partial: 'shared/flash') %>").slideDown();
<% flash.clear %>