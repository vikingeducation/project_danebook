<main class="main timeline">
  <%= render partial: 'shared/profile_header_and_links', locals: { user: @current_user } %>

  <div class="container">
    <div class="bottom-main-section row">
      <section class="left-section col-xs-4">

      <%= render 'users/timeline/about_block' %>

        <%= render 'users/timeline/photo_block' %>

        <%= render 'users/timeline/friend_block' %>

      </section>

      <section class="right-section col-xs-8">

        <%= render 'shared/make_a_post' %>

        <%= render 'users/timeline/post' %>

        <%= render 'users/timeline/post' %>

        <%= render 'users/timeline/post' %>

        <%= render 'users/timeline/post_with_comment' %>

      </section>
    </div>
  </div>

</main>
