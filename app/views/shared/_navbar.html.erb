<nav class="nav navbar-default navbar-fixed-top" id="navbar-custom">
    <div class="container">
        <%= link_to "Bookish", root_path, class: 'navbar-brand', id: "navbar-brand" %>
        <ul class="nav navbar-nav nav-search">
            <li>
                <%= form_tag users_path, :method => 'get', class: "form-inline navbar-form" do %>
                <div class="form-group">
                    <%= text_field_tag :query, nil, placeholder: "Find Users", class: 'form-control'%>
                    <%= submit_tag "Search", class: "btn btn-primary" %>
                </div>
                <% end %>
            </li>
        </ul>

        <ul class="nav navbar-nav navbar-right line-inline">
            <li class="dropdown">
                <%= link_to current_user.profile.first_name, current_user, class: "dropdown-toggle user-name", "data-toggle": "dropdown" %>
                <ul class="dropdown-menu">
                    <li>
                        <%= link_to "My Timeline", current_user %>
                    </li>
                    <li>
                        <%= link_to "Logout", logout_path, :method => :delete %>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>
