<% @friends.each do |friend| %>
	<div class="col-sm-3">
	  <%= image_tag("default.png", :class => "img-thumbnail") %>
	  <div class="col-sm-12">
			<%= link_to friend.name, user_posts_path(friend) %>
	  </div>
	</div>

<% end %>