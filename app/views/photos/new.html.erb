<%= render :partial => 'shared/nav' %>
<%= render :partial => 'shared/profile_header', :locals => {:user => current_user} %>
<%= render :partial => 'shared/flash' %>

<!-- Profile Page / Photos -->
<section id="photos" class="row profile-page-container">
  
  <header class="col-xs-12 profile-page-header">
    <h1>

      <%= current_user.name %>: Add Photo

    </h1>
  </header>

  <!-- Photos -->
  <div class="row">
    <div class="col-xs-12">

      <%= render :partial => 'form', :locals => {:photo => @photo} %>

    </div><!-- col -->
  </div><!-- row -->

</section><!-- #photos -->