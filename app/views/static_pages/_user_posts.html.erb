<% posts.each do |post| %>
<section class="row section-container">
  <div class="col-sm-12">
    <div class="row post-wrapper">
      <div class="col-xs-2 col-sm-1">
      <%= image_tag("harry_potter_small.jpg", class: "post-pic") %>
      </div>
      <div class="col-xs-9 col-sm-10 friend-name-wrapper">
        <p><%= link_to @user.full_name, user_profile_path(@user, @user.profile), class: "friend-name" %></p>
        <p>Posted on <%= post.updated_at.strftime("%A %-m/%-d/%Y") %></p>
      </div>
    </div>
    <div class="row post-wrapper">
      <div class="col-xs-12">
        <p class="post-content"><%= post.description %></p>
      </div>
    </div>
    <div class="row post-wrapper section-footer">
      <div class="col-xs-7 col-sm-4">
        <% if post.likers.include?(current_user) %>
          <%= button_to "Unlike", like_path( user_liked_id(post)), method: :delete, class: "friend-name"%>
        <% else %>
          <%= button_to "Like", post_likes_path(post), method: :post, class: "friend-name" %>
        <% end %>
        <div class="col-xs-7 col-sm-7 text-right">
          <% if @user == current_user %>
            <%= button_to "Delete", post_path(post), method: :delete %>
          <% end %>
  			</div>
        <a class="friend-name" href="#" disabled="true">Comment</a>
      </div>
      <div class="row">
        <div class="col-xs-12 like-listing-expanded hr-block">
            <p><%= print_likers(post) %></p>
            <hr>
        </div>
      </div>
      <div class="col-xs-12">
  			<div class="row post-wrapper">
  				<div class="col-xs-2 col-sm-1">
  					<img class="post-pic" src="http://placehold.it/70x70">
  				</div>
  				<div class="col-xs-10 col-sm-11 left-gutter">
            <% post.comments[0..6].each do |comment| %>
              <%= render 'comment', comment: comment, post: post %>
            <% end %>
  				</div><!-- Comment content wrapper -->
  			</div><!-- Comment wrapper -->
      </div>
    </div>
  </section><!-- Old Post Section -->
<% end %>
