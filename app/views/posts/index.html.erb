  <div class="container" id="timeline-page">
    <div class="row">

      <div class="col-sm-4" id="left-column">

        <%= render partial: 'timeline_info', locals: { page_owner: @page_owner } %>

      </div>
      <!--right column-->

      <!--post creation section-->
      <div class="col-sm-7 col-sm-offset-1" id="right-column">

        <%= render partial: "new_post_form", locals: { page_owner: @page_owner, post: @post } %>

        <!-- posts  -->
        <%= render partial: "all_posts", locals: { posts: @posts } %>

    </div>  
  </div>

