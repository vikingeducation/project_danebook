<div class="top-nav">
  <nav class="navbar navbar-fixed-top navbar-default">
    <div class="container">
      <div class="row">
        <div class="col-md-10 col-md-offset-1">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag('user/logo.gif', class: "logo"), '#', class: "navbar-brand" %>
            <%= link_to 'Danebook', '#', class: "navbar-brand", id: "brand" %>
          </div>

          <div class="collapse navbar-collapse" id="navbar">
            <% if signed_in_user? %>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">User<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "About", user_path(current_user) %></li>
                    <li role="separator" class="divider"></li>
                    <li><%= link_to "Logout", logout_path, method: "delete" %></li>
                  </ul>
                </li>
              </ul>
              <% else %>
                <%= form_tag sessions_path, method: :post ,class: "navbar-form navbar-right" do %>
                  <div class="form-group">
                    <%= text_field_tag :email, "",class: "form-control", placeholder: "email" %>
                  </div>
                  <div class="form-group">
                    <%= password_field_tag :password, "" ,class: "form-control", placeholder: "password" %>
                  </div>
                  <div class="form-group">
                    <%= submit_tag "Log in", class: "btn btn-danger btn-block" %>
                  </div>
                <% end %>

              <% end %>
          </div>
        </div>
      </div>
    </div>
  </nav>
</div>
