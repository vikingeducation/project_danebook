<% if current_user == resource.user %>

  <%= link_to "Delete", link_path,
                        method: :delete,
                        class: 'pull-right',
                        data: {
                          confirm: "Are you sure you would like to delete this resource? This action cannot be undone"
                        } %>
                        
<% end %>
