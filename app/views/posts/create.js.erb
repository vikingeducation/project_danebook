console.log('inside create.js.erb')
var recentPost = "<%= j( render partial: 'post', locals: { post: @post } ) %>"
var $recentPost = $( recentPost );

var $list = PostModule.getPostList();
$list.prepend( $recentPost );

//clear post form
$('.post-text-area textarea').val("")

$("#flash-messages").html("<%= escape_javascript(render partial: 'shared/flash') %>").slideDown();
<% flash.clear %>