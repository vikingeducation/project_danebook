<!--NAVIGATION-->
<nav class="navbar navbar-custom navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#login-navbar-collapse" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <div class="logo-wrapper" >
        <%= link_to signed_in_user? ? user_newsfeed_path(current_user) : root_path, class: "navbar-brand logo-container" do %>
        <%= image_tag("danish_flag.gif") %>
          <h3>Danebook</h3>
        <% end %>
      </div>
    </div>


    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="login-navbar-collapse">
      <%= form_tag(user_search_path(current_user), :method => 'get', class: "navbar-form navbar-left") do %>
          <div class="form-group">
              <%= text_field_tag :search, params[:search], :placeholder => "Search for User", :id => "search-area", :class => "form-control" %>
          </div>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "#{current_user.profile.first_name} Log Out", logout_path, :method => :delete %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->



  </div><!-- /.container -->
</nav>
