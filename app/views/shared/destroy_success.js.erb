console.log('inside destroy success');

if (<%= !flash[:success].blank? %>){
  var dataID = "<%=thing.class%>-<%= thing.id%>"
  var $thingToDestroy = $('article[data-id='+ dataID+']');
  $thingToDestroy.slideUp(500, function(){this.remove();});
}

// PostModule.displayFlash();

$("#flash-messages").html("<%= escape_javascript(render partial: 'shared/flash') %>").hide().slideDown();
<% flash.clear %>