


$form = $('article[data-content="<%= @comment.commentable_type.to_s %>-<%=@comment.commentable_id %>"] .new-comment-field')

console.log($form)

$form.before("<%= j render partial: 'profiles/timeline_partials/comment', :locals => {:comment => @comment} %>").prev().hide().slideDown()

$form.children('textarea').val("")
