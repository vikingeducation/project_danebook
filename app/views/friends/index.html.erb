<%= render 'users/user_header', user: @user %>

<div class="friends-list">
  <article>
  <% @friends.each do |friend| %>
      <%= link_to (image_tag friend.headshot_pic), user_feed_path(friend) %>
  <% end %>
  </article>
</div> <!-- row -->
