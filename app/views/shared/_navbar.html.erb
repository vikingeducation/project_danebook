<div class="top-nav">
  <nav class="navbar navbar-fixed-top navbar-default">
    <div class="container">
      <div class="row">
        <div class="col-md-10 col-md-offset-1">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to 'Danebook', root_path , class: "navbar-brand", id: "brand" %>
          </div>



          <div class="collapse navbar-collapse" id="navbar">
            <% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-right">
              <li><%= link_to raw("<i class='fa fa-newspaper-o' aria-hidden='true'></i> News"), newsfeed_path(current_user) %></li>
              <li><%= link_to raw("<i class='fa fa-user' aria-hidden='true'></i> Timeline"), timeline_path(current_user) %></li>
              <li><%= link_to raw("<i class='fa fa-users' aria-hidden='true'></i> Friends"), friends_path(current_user) %></li>
              <li><%= link_to raw("<i class='fa fa-camera' aria-hidden='true'></i> Photos"), photos_path(user_id: current_user) %></li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user-md" aria-hidden="true"></i> <%= current_user.username %><span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "About", user_path(current_user) %></li>
                  <li role="separator" class="divider"></li>
                  <li><%= link_to raw("<i class='fa fa-sign-out' aria-hidden='true'></i> Logout"), destroy_user_session_path, method: "delete" %></li>
                </ul>
              </li>
            </ul>

            <%= render 'shared/search' %>

            <% else %>
              <!-- render the sign in form -->
            <%= render 'devise/sessions/new' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </nav>
</div>
